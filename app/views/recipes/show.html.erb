<h1><%= @recipe.name %></h1>
<h3><%= @recipe.tag %></h3>
<h1><%= @recipe.rating %></h1>
<h3><%= @recipe.instructions %></h3>

<% if @recipe.ingredients.any? %>
  <h4>Here are the ingredients for this recipe:</h4>

  <% @recipe.ingredients.each do |ingredient| %>
    <ul>
      <li>
        <%= link_to ingredient.name, recipe_ingredient_path(@recipe, ingredient) %>
      </li>
    </ul>
  <% end %>
<% else %>
  <p>No ingredients added yet for this recipe.</p>
<% end %>

<p><%= link_to "Add an ingredient", new_ingredient_path(@recipe) %></p>

<p><%= link_to "Edit", edit_recipe_path(@recipe) %></p>
<p><%= link_to "Delete recipe", recipe_path(@recipe),
                        :data => {:confirm => "Are you sure?",
                                       :method => "delete"} %></p>

<p><%= link_to "Return to recipes", recipe_path %></p>